// Layer: UI
// Type: Server Component — mock UI only
// RLS: N/A — mock data

export default function SocialPage() {
  return (
    <div className="max-w-7xl mx-auto px-8 py-12">
      <h1 className="text-2xl font-semibold tracking-tight text-[var(--text-primary)] mb-8">
        Social Hub
      </h1>
      <section aria-label="Social Hub content">
        {/* Mock UI will be added in task 7.2 */}
      </section>
    </div>
  );
}
